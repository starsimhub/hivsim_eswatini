[project]
name = "hivsim-eswatini"
version = "0.1.0"
description = "HIV calibration project for Eswatini, built on Starsim/STIsim"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "starsim>=3.5.2",
    "stisim>=1.5.10",
    "numpy",
    "pandas",
    "matplotlib",
    "seaborn",
    "scipy",
    "sciris",
    "optuna",
    "openpyxl",
    "pyarrow",
]

[dependency-groups]
dev = [
    "ipykernel",
    "jupyter",
    "ruff",
]

[tool.uv.sources]
starsim = { path = "../../star_sim/starsim", editable = true }
stisim = { path = "../../star_sim/stisim", editable = true }
