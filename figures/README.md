# figures/ — scratch directory

Plot scripts (e.g. `plot_dashboard.py`) write here. Contents are gitignored and overwritten freely between runs.

When you close out an experiment, copy the figures you want to keep into `experiments/NNN_topic/figures/` and document the change in `experiments/log.md`.
